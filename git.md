```bash
# A branch 생성
$ git branch A

# A로 HEAD 변경
$ git switch A

# A branch 생성 및 HEAD로 이동
$ git switch -c A

# A branch 삭제
$ git branch -d A

# 병합
$ git merge A

$ git log --oneline

$ git log --oneline --graph
```

